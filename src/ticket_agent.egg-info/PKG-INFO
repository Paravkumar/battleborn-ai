Metadata-Version: 2.4
Name: ticket-agent
Version: 0.1.0
Summary: Hackathon MVP for customer ticket resolution with Ollama-backed planning.
Requires-Python: >=3.11
Description-Content-Type: text/markdown
Requires-Dist: ollama>=0.4.7
Requires-Dist: pydantic<3.0,>=2.7
Provides-Extra: dev
Requires-Dist: pytest>=8.0; extra == "dev"
Provides-Extra: train
Requires-Dist: accelerate>=0.34; extra == "train"
Requires-Dist: bitsandbytes>=0.43; extra == "train"
Requires-Dist: datasets>=2.20; extra == "train"
Requires-Dist: peft>=0.12; extra == "train"
Requires-Dist: transformers>=4.45; extra == "train"
Requires-Dist: trl>=0.10; extra == "train"

# Ticket Agent MVP

This repository contains a first prototype for the customer ticket resolution workflow domain. The architecture is optimized for a 24-hour hackathon:

- Python controls the workflow and tool execution.
- Ollama provides the student model for planning and grounded drafting.
- Mock tools simulate the ticketing system, KB access, posting, status updates, retries, and escalation.
- Every run emits a structured execution log that can later be converted into a fine-tuning dataset.

## What is implemented

- Plan-then-execute agent loop
- Allowed tool registry for the customer support domain
- Retry handling with max two retries per step
- Graceful escalation if a step cannot be resolved
- JSON execution logs saved to `outputs/`
- Sample tickets and KB articles for three demo scenarios
- SFT export command to convert a run log into a training example

## Quick start

1. Create and activate a virtual environment.
2. Install the project:

```powershell
py -m venv .venv
.venv\Scripts\Activate.ps1
pip install -e .[dev]
```

3. Run a smoke demo without Ollama:

```powershell
ticket-agent run --ticket-id TICK-1001 --disable-ollama
```

4. Run with Ollama and the intended student model:

```powershell
$env:OLLAMA_STUDENT_MODEL = "llama3.1:8b"
ticket-agent run --ticket-id TICK-1001
```

## Scale up synthetic data

Generate a larger synthetic ticket file:

```powershell
.venv\Scripts\python.exe scripts\generate_synthetic_tickets.py --access-count 30 --billing-count 30 --integration-count 20 --escalation-count 20 --output artifacts\generated_tickets.json
```

Run the teacher exporter against that file:

```powershell
.venv\Scripts\python.exe scripts\generate_teacher_runs.py --all-tickets --tickets-file artifacts\generated_tickets.json --model llama3.1:8b --require-ollama-plan --require-ollama-draft --output-jsonl artifacts\teacher_runs.jsonl
```

## Demo scenarios

- `TICK-1001`: clean password reset resolution
- `TICK-1002`: transient KB lookup failure followed by success
- `TICK-1003`: no grounded resolution, escalates after retries

## Fine-tuning path

1. Run the agent on many synthetic or curated tickets.
2. Export the execution logs to SFT examples.
3. Fine-tune `Llama-3.1-8B-Instruct` with LoRA outside Ollama.
4. Import the adapter back into Ollama with a `Modelfile`.

The initial training notes are in [training/README.md](training/README.md).
